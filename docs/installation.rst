.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install trenitapy

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv trenitapy
    $ pip install trenitapy
